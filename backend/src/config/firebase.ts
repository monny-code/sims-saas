export const firebaseApp = null;
export const firestore = null;
export const firebaseAuth = null;
export const isFirebaseEnabled = false;

export const firebaseConfigStatus = 'Firebase is disabled for this Supabase-first deployment.';
